# 点购 Agent OS 依赖
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9     # FastAPI form / file upload
apscheduler>=3.10           # 每日自动刷新 scheduler
requests>=2.31
websocket-client>=1.7        # ERP token 抓取（绕开 playwright）
playwright>=1.40             # ERP token 兜底路径

# LLM providers — 按需安装
anthropic>=0.40              # Anthropic / Claude（本地 OAuth keychain）
openai>=1.50                 # Qwen / DeepSeek / 豆包 OpenAI 协议兼容

# 导出 — chat/UI 的 export_table 生成 xlsx（销量「最后输出数据」导出，WS-20）
openpyxl>=3.1

# DB
psycopg2-binary>=2.9      # Postgres（DB_URL 设置时启用）
# sqlalchemy>=2.0          # 暂未使用，留给阶段 2 任务队列

# Auth (W2) — 用 pbkdf2 而非 bcrypt（bcrypt 5.x 与 passlib 不兼容）
python-jose[cryptography]>=3.3
passlib>=1.7
