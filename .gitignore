.DS_Store
.~*
*.xlsx
*.numbers
*.db
*.db.backup-*
inbox/
logs/

# Python 缓存
__pycache__/
*.pyc

# 大体量数据目录 — 不进 git（运行时产物）
selection/
refs/

# 选品 Agent 凭证 — 不要 commit
点购_选品_凭证.md
.chrome-profiles/

# 各种 secret env（凭据不能进 git）
.env
.env.local
.env.production

# 飞书 OAuth token / 任何凭据文件
feishu_auth/token.json
feishu_auth/*.json
*.token
*.credentials.json
